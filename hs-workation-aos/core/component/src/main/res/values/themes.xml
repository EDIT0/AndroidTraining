<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Bottom sheet dialog Style -->
    <style name="CustomBottomSheetDialogTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomRoundedBackgroundBottomSheet</item>
    </style>

    <style name="CustomRoundedBackgroundBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
    </style>

    <style name="CustomRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/custom_ratingbar</item>
        <item name="android:indeterminateDrawable">@drawable/custom_ratingbar</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:maxHeight">48dp</item>
    </style>

    <style name="CustomNumberPicker" parent="">
        <item name="android:textColorPrimary">@color/grey_900</item> <!-- 선택된 텍스트 색 -->
        <item name="android:textSize">17sp</item>
        <item name="colorControlNormal">@color/grey_500</item> <!-- Divider 색 -->
    </style>
</resources>